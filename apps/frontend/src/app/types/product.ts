export type Product = {
  id: string
  sku: string
  name: string
  description: string
  category: string
  tags: string[]
  price: string
  stock: number
  imageUrl: string
  createdAt: string
  updatedAt: string
}
